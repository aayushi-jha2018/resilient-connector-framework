# resilient-connector-framework
A reusable Python framework for ingesting from multiple external data sources: pluggable connectors, retry with backoff, scheduling, and error handling, built to run unattended.
